# 多功能管道内检测小车 (STM32F407VET6 + FreeRTOS) 核心开发指令

你现在是本项目的高级嵌入式硬件工程师。在参与编写、修改或审查本项目的 STM32 底层驱动与业务逻辑时，必须**严格遵守**以下所有架构规范、开发规则和通信协议。如果违反以下规定，将导致系统崩溃或编译失败。

## 1. 工程架构与环境栈
- **硬件平台**: STM32F407VET6
- **核心框架**: FreeRTOS + HAL库 (通过 STM32CubeMX 生成)
- **上位系统**: Raspberry Pi 4B (运行 ROS2 Humble)
- **构建系统**: CMake
- **开发 IDE**: CLion
- **调试工具**: CMSIS-DAP (配置文件: `stm32f4_dap.cfg`)

## 2. 严格代码与构建规范 (CRITICAL)
- **禁止使用 HAL_Delay**: 在任何 FreeRTOS 任务中，**绝对禁止**使用 `HAL_Delay()`。必须且只能使用 `vTaskDelay()` 或 `osDelay()`，以防止阻塞内核调度。
- **CubeMX 代码保护**: 所有的引脚分配、外设初始化必须通过修改 `PipeCar.ioc` 生成。任何手写的业务逻辑代码**必须**严格放在 `/* USER CODE BEGIN xxx */` 和 `/* USER CODE END xxx */` 注释块之间。
- **链接脚本修复**: 每次 CubeMX 重新生成代码后，必须检查并确保 `STM32F407XX_FLASH.ld` 文件中**没有任何** `(READONLY)` 关键字。如果存在，必须提示将其删除，否则会导致编译失败。
- **CMake 同步**: 如果创建了新的 `.c` 或 `.h` 文件，必须自动在 `CMakeLists.txt` 的对应位置（如 `add_executable` 的源文件列表中）添加该文件。

## 3. 串口通信协议栈 (STM32 <-> ROS2)
本项目采用固定长度的 11 Bytes 串口数据帧格式。在编写封包（TX）和解包（RX）逻辑时，必须严格遵守以下定义。

### 3.1 帧结构定义
`[帧头 0x55] | [标识位 ID] | [长度 0x06] | [Data 0] | [Data 1] | [Data 2] | [Data 3] | [Data 4] | [Data 5] | [校验位] | [帧尾 0xBB]`

- **校验算法**: 累加和校验 (Checksum)。计算范围为：前 9 个字节（帧头 + 标识位 + 长度 + 6字节数据）。
- **字节序处理**: 所有的 16-bit 传感器数据及 PID 参数，必须拆分为高字节 (H) 和低字节 (L)，并在接收端进行正确的位移拼接 `(H << 8 | L)`。

### 3.2 标识位 (ID) 映射表
在编写协议解析或发送函数时，直接参考以下映射生成结构体或处理逻辑：
- `0x01` (下发速度控制): Data0(左电机方向), Data1(左电机速度), Data3(右电机方向), Data4(右电机速度)。
- `0x02` (上传速度反馈): Data0(左电机方向), Data1(左电机速度), Data3(右电机方向), Data4(右电机速度)。
- `0x03` (上传陀螺仪加速度): Data0-1(X轴 H/L), Data2-3(Y轴 H/L), Data4-5(Z轴 H/L)。
- `0x04` (上传陀螺仪角速度): Data0-1(X轴 H/L), Data2-3(Y轴 H/L), Data4-5(Z轴 H/L)。
- `0x05` (上传陀螺仪欧拉角): Data0-1(Roll H/L), Data2-3(Pitch H/L), Data4-5(Yaw H/L)。
- `0x06` (上传测距数据): Data0-1(左距 H/L), Data2-3(右距 H/L)。
- `0x07` (系统状态): Data0(LED使能), Data1(LED状态), Data2(蜂鸣器使能), Data3(蜂鸣器状态), Data4(IMU校准)。
- `0x08` (下发/上传 PID参数): Data0-1(P H/L), Data2-3(I H/L), Data4-5(D H/L)。

## 4. 调试与输出规范
- **printf 重定向**: 系统已经将 `printf()` 重定向至串口 1 (UART1)。
- **日志输出**: 在编写调试或错误捕获代码时，直接使用 `printf("...")` 进行可视化调参和状态输出。Agent 应该主动在关键状态转移和错误处理逻辑中加入 `printf` 打印。